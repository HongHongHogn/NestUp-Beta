{
  "name": "nestup-ai-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "NestUp AI Backend API Server",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node server.js",
    "dev": "nodemon server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:deploy": "prisma migrate deploy",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8",
    "morgan": "^1.10.0",
    "express-rate-limit": "^7.4.0",
    "@prisma/client": "^5.19.1",
    "pg": "^8.11.5",
    "openai": "^4.56.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.19.1"
  }
}
